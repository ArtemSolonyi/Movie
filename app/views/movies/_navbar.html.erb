<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href=""><%= image_tag ("m-logo-512x512.png"), width: 15, height: 100, class: "brand_logo" %></a>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <%= form_with url: movies_path, method: :get do |form| %>
          </li>
          <li class="nav-item">
            <a class="nav-link">
              <%= form.collection_select :id, @categories, :id, :name, { selected: params[:id] } %>
              <%= form.submit 'Switch', class: "btn btn-primary" %>
        <% end %>
        </a>
        </li>
        <li class="nav-item">
          <% if user_signed_in? %>
            <a class="nav-link">
              <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
          <% else %>
            <a class="nav-link position_add">
              <%= link_to 'Log in', new_user_session_path, class: " btn btn-success" %>
          <% end %>
          </a>
          </a>
        </li>
        <% if current_user %>
          <li class="nav-item ">
            <a class="nav-link position_add">
              <%= link_to_if current_user.email == "my@mail.com", '+ Add Movie', { controller: "movies", action: :new }, class: "btn btn-warning " %>
            </a>
        <% end %>
        </li>
        </ul>
  </div>
</nav>
